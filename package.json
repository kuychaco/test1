{
  "name": "test",
  "main": "./lib/test",
  "version": "0.0.0",
  "description": "A short description of your package",
  "keywords": [
  ],
  "activationCommands": {
    "atom-workspace": "test:toggle"
  },
  "repository": "https://github.com/atom/test",
  "license": "MIT",
  "engines": {
    "atom": ">=1.0.0 <2.0.0"
  },
  "dependencies": {
  },
  "configSchema": {
    "darkThemes": {
      "type": "array",
      "default": ["one-dark-ui", "one-dark-syntax"],
      "items": {
        "type": "string"
      }
    },
    "lightThemes": {
      "type": "array",
      "default": ["one-light-ui", "one-light-syntax"],
      "items": {
        "type": "string"
      }
    }
  }
}
